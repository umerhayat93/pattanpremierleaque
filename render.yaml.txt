services:
  - type: web
    name: ppl2026
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --workers 2 --threads 4 --worker-class gthread --timeout 120 --bind 0.0.0.0:$PORT
    envVars:
      - key: DB_PATH
        value: /data/ppl.db
    disk:
      name: ppl-data
      mountPath: /data
      sizeGB: 1
