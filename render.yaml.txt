services:
  - type: web
    name: ppl2026
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app -w 1 -k gthread --threads 20 --timeout 300 -b 0.0.0.0:$PORT
    envVars:
      - key: DB_PATH
        value: /data/ppl.db
    disk:
      name: ppl-data
      mountPath: /data
      sizeGB: 1
